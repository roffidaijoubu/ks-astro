---
let {footnotes} = Astro.props;
---
{
    footnotes && (
        <section class="prose font-serif lg:max-w-none mb-5 mt-4 py-4 border-t-[1px] border-base-content/20 text-sm mx-11">
            <h3 id="footnotes">Catatan Kaki</h3>
            <ol>
                {footnotes.map((footnote) => (
                    <li id={`${footnote.id}`}>
                        <div set:html={footnote.content} />{" "}
                        <a
                            href={`#${footnote.id}-link`}
                            class="text-base-content/50"
                        >
                            ↩
                        </a>
                    </li>
                ))}
            </ol>
        </section>
    )
}